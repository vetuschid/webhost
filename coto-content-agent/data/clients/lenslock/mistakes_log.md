# Mistakes Log

This file accumulates patterns the system has seen go wrong, so future runs
can avoid them. The QA Agent appends here when it logs failures, and the
admin (CEO Agent) can curate it manually.

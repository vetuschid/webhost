## Description

(What does this PR do? Link to related issue if any.)

## Checklist

- [ ] `npm run build` passes in `cli/`
- [ ] `npm test` passes in `cli/`
- [ ] CHANGELOG updated if this is a user-facing change (see [docs/RELEASING.md](docs/RELEASING.md))

## Notes

(Any breaking changes, follow-up work, or context for reviewers.)
